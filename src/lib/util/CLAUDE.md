# src/lib/util

This directory should contain small, self-contained files that export simple utility functions — pure, dependency-light helpers (e.g. clamping, formatting, validation) that don't touch the database, SvelteKit context, or other app-specific state. Prefer several focused files over one grab-bag `utils.ts`.
